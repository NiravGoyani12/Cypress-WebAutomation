// eslint-disable-next-line
/// <reference path="../../support/index.d.ts" />

before(() => {
    // Will run once, before all tests
});

beforeEach(() => {
    // Will run once, before each test
});

after(() => {
    // Will run after all tests done
});
